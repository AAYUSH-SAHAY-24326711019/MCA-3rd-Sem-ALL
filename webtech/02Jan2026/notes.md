javascript and jquery
jquery is a do more or the write less js library
This is importand for the DOM manipulations
event handling 
css animation

Dom has the purpose , the webpage html , any component could be
accessed and do the modification at the run time

To change the text of the datatype.

p tag / h1 tag is access by the selecter.
then document.getByID/ get By name and then the data inside the 
tag could be manipulated / added.

That same can be done by the jquery.


AJAX : asynchronous / Javascript and Xml

no need to refresh the site over and over again.
Continuously all the page show the updates. This is used for the api calls
This will do the data fetch from the data base. for the api

This will remove the page refresh problem .The biggest problem of js is that 
it is not cross browser compatible.

Jquery will allow the cross border compatibility.

